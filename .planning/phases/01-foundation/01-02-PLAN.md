---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - components/Header.tsx
  - app/layout.tsx
  - app/page.tsx
autonomous: true
must_haves:
  truths:
    - "Header displays OIL logo, nav links (About, Explore, Docs), social buttons (Twitter, GitHub, Discord)"
    - "Header displays wallet connect button (stub - shows 'Connect Wallet' or similar)"
    - "Page transitions are smooth when navigating"
  artifacts:
    - path: components/Header.tsx
      provides: "Global header component"
      contains: "About"
    - path: app/layout.tsx
      provides: "Layout with Header"
      contains: "Header"
  key_links:
    - from: app/layout.tsx
      to: components/Header.tsx
      via: "import and render"
      pattern: "Header"
---

<objective>
Build the global Header component and integrate into layout. Add smooth page transitions.

Purpose: Complete LAYOUT-01, LAYOUT-02, and success criterion 4 (smooth transitions).
Output: Header visible on all pages; wallet stub; transitions configured.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

Plan 01-01 provides: Next.js app, dark theme, root layout.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Header component</name>
  <files>components/Header.tsx</files>
  <action>
    Create Header component with:
    - Left: OIL logo (text "OIL" or simple SVG/emoji placeholder; can use 🔶 or similar)
    - Nav links: About, Explore, Docs (use Next.js Link, routes /about, /explore, /docs - create placeholder pages if needed)
    - Right: Twitter, GitHub, Discord icons/links (external URLs - use placeholders like # or ore.supply-style links)
    - Right: "Connect Wallet" button (stub - onClick can be no-op or console.log for MVP)
    Style: Dark premium, subtle borders, hover states. Use Tailwind. Match theme variables from globals.css.
    Responsive: Stack or collapse on smaller screens (hamburger optional for MVP - simple flex wrap OK).
  </action>
  <verify>Header renders; all links/buttons visible; no layout shift</verify>
  <done>Header displays logo, nav, social, wallet stub per LAYOUT-01, LAYOUT-02</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Header into layout</name>
  <files>app/layout.tsx</files>
  <action>
    Import Header in app/layout.tsx. Render Header above main content (or as fixed/sticky if preferred).
    Wrap children in main tag with appropriate padding for content below header.
    Ensure header is always visible (e.g. position sticky top-0 with z-10).
  </action>
  <verify>Header appears on all routes; content flows below it</verify>
  <done>Header integrated; visible on every page</done>
</task>

<task type="auto">
  <name>Task 3: Add smooth page transitions</name>
  <files>app/layout.tsx, next.config.ts</files>
  <action>
    Enable smooth transitions for route changes. Options:
    - Next.js 14 View Transitions: Add experimental.optimizePackageImports or view transitions if available
    - Or use CSS transitions on layout children: add transition-all duration-300 to main content wrapper
    - Or install next-view-transitions / similar lightweight lib
    Simplest: Add a template.tsx or wrap children with a div that has transition classes; or use Next.js built-in if documented.
    Goal: Navigating between About/Explore/Docs feels smooth, not jarring.
  </action>
  <verify>Navigate between /, /about, /explore, /docs; transitions feel smooth</verify>
  <done>Page transitions are smooth when navigating</done>
</task>

</tasks>

<verification>
- [ ] npm run build succeeds
- [ ] Header visible on all pages
- [ ] Nav links work (placeholder pages for About, Explore, Docs)
- [ ] Wallet button renders
- [ ] Transitions smooth
</verification>

<success_criteria>
- Header displays logo, nav links, social buttons, wallet connect stub
- Dark premium theme consistent in header
- Page transitions smooth
</success_criteria>

<output>
After completion, create .planning/phases/01-foundation/01-02-SUMMARY.md
</output>
