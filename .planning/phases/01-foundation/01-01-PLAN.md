---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
  - tailwind.config.ts
  - postcss.config.mjs
  - tsconfig.json
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
autonomous: true
must_haves:
  truths:
    - "Next.js app runs locally with npm run dev"
    - "Tailwind CSS is configured and working"
    - "Dark premium theme (dark backgrounds, subtle gradients) is applied globally"
  artifacts:
    - path: package.json
      provides: "Next.js project with dependencies"
      contains: "next"
    - path: app/layout.tsx
      provides: "Root layout"
      contains: "RootLayout"
    - path: app/globals.css
      provides: "Global styles with dark theme"
      contains: "background"
    - path: tailwind.config.ts
      provides: "Tailwind configuration"
      contains: "content"
  key_links: []
---

<objective>
Scaffold Next.js 14+ project with App Router, TypeScript, and Tailwind. Configure dark premium aesthetic as the global default.

Purpose: Establish the foundation for OLIO with correct tooling and visual direction.
Output: Runnable Next.js app with dark theme, ready for header integration.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Constraints: Next.js App Router, TypeScript, Tailwind CSS. Dark + premium aesthetic.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Next.js project with Tailwind</name>
  <files>package.json, next.config.ts, tailwind.config.ts, postcss.config.mjs, tsconfig.json</files>
  <action>
    Run create-next-app in the olio-app directory. Use:
    - Next.js 14+ with App Router
    - TypeScript
    - Tailwind CSS
    - ESLint
    - No src directory (use app/ at root)
    - Import alias @/*
    Use non-interactive flags: --yes or equivalent for create-next-app.
    If directory has existing files (README, .planning), run create-next-app in current dir with . as target.
    Preserve .planning/ and existing assets (README, images) - do not overwrite.
  </action>
  <verify>npm run dev starts successfully; localhost:3000 returns 200</verify>
  <done>Next.js app runs; Tailwind compiles; default page loads</done>
</task>

<task type="auto">
  <name>Task 2: Configure dark premium theme</name>
  <files>app/globals.css, tailwind.config.ts</files>
  <action>
    In app/globals.css: Set dark background (e.g. #0a0a0f or similar near-black), base text color white/light gray.
    Add CSS variables for theme colors: --bg-primary, --bg-elevated, --accent (gold/amber for OIL brand), --text-primary, --text-muted.
    Use subtle gradients where appropriate (e.g. radial gradient for depth).
    In tailwind.config.ts: Extend theme with these CSS variables. Set darkMode: 'class' or 'selector' if needed, but default the app to dark (no toggle yet).
  </action>
  <verify>Page renders with dark background; text is readable; no white flash</verify>
  <done>Dark premium aesthetic applied globally; CSS variables available for components</done>
</task>

<task type="auto">
  <name>Task 3: Set up root layout structure</name>
  <files>app/layout.tsx, app/page.tsx</files>
  <action>
    In app/layout.tsx: Import globals.css. Use Geist or similar modern font. Set html/body to full viewport, dark background from theme.
    In app/page.tsx: Replace default content with minimal placeholder (e.g. "OLIO" heading) so we have a clean slate for Plan 01-02.
  </action>
  <verify>npm run build succeeds; page renders without errors</verify>
  <done>Root layout ready for Header; placeholder home page displays</done>
</task>

</tasks>

<verification>
- [ ] npm run build succeeds
- [ ] npm run dev runs without errors
- [ ] Page at localhost:3000 shows dark background
- [ ] No console errors
</verification>

<success_criteria>
- Next.js app runs locally with Tailwind configured
- Dark premium theme applied globally
- Build passes
</success_criteria>

<output>
After completion, create .planning/phases/01-foundation/01-01-SUMMARY.md
</output>
