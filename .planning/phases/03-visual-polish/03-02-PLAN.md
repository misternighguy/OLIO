---
phase: 03-visual-polish
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - components/MotherlodeOverlay.tsx
  - components/DrillingGrid.tsx
  - components/DrillTile.tsx
  - app/page.tsx
  - lib/game-state.tsx
autonomous: true
must_haves:
  truths:
    - "Motherlode triggers dramatic full-screen moment"
    - "Loading states are polished"
  artifacts:
    - path: components/MotherlodeOverlay.tsx
      provides: "Full-screen Motherlode celebration"
      contains: "Motherlode"
  key_links:
    - from: components/DrillingGrid.tsx
      to: components/MotherlodeOverlay.tsx
      via: "trigger on motherlode outcome"
      pattern: "MotherlodeOverlay|motherlode"
---

<objective>
Create Motherlode full-screen overlay and polish loading states. GRID-07, VIS-04.

Purpose: Jackpot moment feels dramatic; loading/empty states are refined.
Output: MotherlodeOverlay component, grid loading polish.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-visual-polish/03-01-SUMMARY.md

From spec: Motherlode = full-screen moment + jackpot counter. Dramatic animation.
</context>

<tasks>

<task type="auto">
  <name>Task 1: MotherlodeOverlay component</name>
  <files>components/MotherlodeOverlay.tsx</files>
  <action>
    Create MotherlodeOverlay: full-screen fixed overlay (z-50), dark backdrop with gold/accent accents. Display "MOTHERLODE!" or similar headline, jackpot amount (passed as prop), optional confetti or particle effect (CSS-only if possible - animated dots or simple keyframes). Auto-dismiss after 3-4 seconds or on click. Fade-in animation (opacity 0->1, scale 0.95->1). Use transition or CSS animation.
  </action>
  <verify>Overlay renders full-screen; dismisses; looks dramatic</verify>
  <done>MotherlodeOverlay component complete</done>
</task>

<task type="auto">
  <name>Task 2: Wire Motherlode trigger</name>
  <files>lib/game-state.tsx, components/DrillingGrid.tsx, app/page.tsx</files>
  <action>
    Add to game state: showMotherlode (boolean), motherlodeAmount (number). When any tile outcome is 'motherlode', set showMotherlode true and motherlodeAmount to payout. Render MotherlodeOverlay in page or layout when showMotherlode; pass amount, onDismiss callback to reset showMotherlode. DrillingGrid (or tile callback) triggers this when outcome is motherlode.
  </action>
  <verify>Drilling a Motherlode tile triggers full-screen overlay</verify>
  <done>GRID-07 satisfied</done>
</task>

<task type="auto">
  <name>Task 3: Polish loading states</name>
  <files>components/DrillingGrid.tsx</files>
  <action>
    Replace "Loading grid…" with a polished skeleton or shimmer. Options: grid of placeholder tiles with pulse animation, or a single centered spinner with "Preparing field…" text. Use CSS animation (animate-pulse or custom keyframe). Ensure no layout shift when grid loads.
  </action>
  <verify>Grid loading state looks polished; no jarring transition</verify>
  <done>VIS-04 satisfied</done>
</task>

</tasks>

<verification>
- [ ] npm run build succeeds
- [ ] Motherlode triggers overlay
- [ ] Overlay dismisses correctly
- [ ] Loading state polished
</verification>

<success_criteria>
- Motherlode full-screen moment works
- Loading states polished
</success_criteria>

<output>
After completion, create .planning/phases/03-visual-polish/03-02-SUMMARY.md
</output>
