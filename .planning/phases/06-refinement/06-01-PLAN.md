---
phase: 06-refinement
plan: 01
type: execute
wave: 1
depends_on: ["05-02"]
files_modified:
  - components/GameLayout.tsx
  - components/Header.tsx
  - app/page.tsx
  - components/ControlsPanel.tsx
  - components/SessionStats.tsx
  - components/ChatPanel.tsx
autonomous: true
must_haves:
  truths:
    - "App works well on tablet-sized screens"
    - "Controls collapse or adapt appropriately on smaller screens"
    - "3-column layout becomes stacked or collapsible on narrow viewports"
  artifacts: []
  key_links: []
---

<objective>
Refine responsive layout for tablet and smaller viewports. Home page 3-column layout should adapt; controls, stats, and chat should remain usable.

Purpose: LAYOUT-04. Tablet and mobile usability.
Output: Responsive game layout, header adjustments.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

Current: GameLayout uses grid-cols-1 (stacked) on mobile, lg:grid-cols-[280px_1fr_280px] on large. Panels stack vertically. Need to ensure tablet (md) has a sensible layout and controls don't overflow or feel cramped.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Game layout breakpoints</name>
  <files>components/GameLayout.tsx</files>
  <action>
    Review breakpoints. Current: grid-cols-1 → lg:grid-cols-[280px_1fr_280px]. Consider md breakpoint: on tablet (768px), 3 columns may be tight. Options: (a) keep stacked until lg, (b) use md for 2-col (controls | grid+stats) and lg for full 3-col. Choose approach that keeps grid prominent. Ensure panel order on mobile: Controls → Grid → Stats+Chat (or Grid first for game-first UX). Adjust grid template and panel ordering if needed.
  </action>
  <verify>Layout adapts smoothly at md and lg breakpoints</verify>
  <done>Layout breakpoints refined</done>
</task>

<task type="auto">
  <name>Task 2: Panel sizing and overflow</name>
  <files>components/GameLayout.tsx, components/ControlsPanel.tsx, components/SessionStats.tsx, components/ChatPanel.tsx</files>
  <action>
    Ensure left and right panels have min/max widths, overflow-y-auto where needed, and don't cause horizontal scroll on narrow screens. ControlsPanel: wrap button groups if needed. SessionStats + ChatPanel: ensure right panel scrolls when tall. Use responsive padding (p-4 md:p-6) consistently.
  </action>
  <verify>No horizontal scroll; panels scroll when needed</verify>
  <done>Panel overflow handled</done>
</task>

<task type="auto">
  <name>Task 3: Header mobile</name>
  <files>components/Header.tsx</files>
  <action>
    On very narrow screens (&lt;640px), header may overflow. Options: (a) hide social icons on sm, (b) shrink Connect Wallet text, (c) hamburger menu for nav. Prefer minimal change: ensure nav and wallet fit. If nav wraps or overflows, consider collapsing to icons or hamburger. Test at 320px, 375px, 768px widths.
  </action>
  <verify>Header usable at 320px and 768px viewport</verify>
  <done>Header responsive</done>
</task>

<task type="auto">
  <name>Task 4: Grid and tile responsiveness</name>
  <files>components/DrillingGrid.tsx, components/DrillTile.tsx</files>
  <action>
    Grid uses max-w-2xl and gap-2 sm:gap-3. Tiles use aspect-square. Ensure grid doesn't overflow on narrow viewports; tiles remain tappable (min touch target ~44px). If needed, reduce gap on small screens. Verify 3x3, 4x4, 5x5 all fit on 375px width.
  </action>
  <verify>Grid fits on narrow screens; tiles tappable</verify>
  <done>Grid responsive</done>
</task>

</tasks>

<verification>
- [ ] npm run build succeeds
- [ ] Layout adapts at md/lg
- [ ] Header usable at 320px
- [ ] Grid fits 5x5 on 375px
</verification>

<success_criteria>
- App works well on tablet and smaller viewports
</success_criteria>

<output>
After completion, create .planning/phases/06-refinement/06-01-SUMMARY.md
</output>
